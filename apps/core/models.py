# The core app has no models of its own.
# It hosts cross-cutting concerns: permissions, exception handling,
# response helpers, and the admin analytics endpoints (which aggregate
# data from the other apps rather than storing anything new).
